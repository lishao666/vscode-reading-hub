# 隐私说明

最后更新：2026-08-06

本项目是非官方第三方本地阅读辅助工具，与腾讯、微信读书、番茄小说及相关内容版权方不存在隶属、合作、授权或官方认可关系。

## 处理的数据

为实现用户主动发起的阅读操作，扩展可能处理：

- 用户主动提供或通过独立登录窗口产生的 Cookie、网页登录信息和 API Key；
- 书籍 ID、章节 ID；
- 当前阅读章节和页码；
- 请求是否成功、HTTP 状态码等必要错误和运行状态信息。

扩展不会在日志中输出完整 Cookie、Authorization、Token、用户 ID、完整请求头或正文接口完整响应。

## 保存位置

- API Key、Cookie 和网页登录授权信息仅保存在 VS Code `SecretStorage`。
- 当前书籍、章节 ID、页码、完整书架和章节目录保存在 VS Code 本地扩展状态或扩展专属全局存储中。
- 番茄小说最近读取或预加载的章节会写入扩展专属本地缓存目录，最多保留最近 20 章；微信读书正文仍只保存在当前扩展进程内存中。
- 番茄多窗口协调使用本机 Unix Socket 或 Windows 命名管道，并通过扩展全局存储中的进度文件通知其他窗口；这些数据不发送到外部网络。
- 性能日志默认关闭；开启后仅写入阶段名称与耗时，不包含正文、Cookie、令牌或其他凭据。

## 数据发送对象

- 数据仅发送到实现用户请求的阅读功能所必要的微信读书或番茄小说相关服务地址。
- 如果用户主动配置 `vscodeReading.proxy` 或 VS Code `http.proxy`，网络请求会经过用户指定的代理；用户应自行确认代理服务的可信度和数据处理方式。
- 不发送到插件作者自建服务器。
- 不出售数据，不共享给广告平台，不上传工作区文件或编辑器内容。
- 正文仅用于用户本地展示，不上传到其他第三方服务器，不用于公开传播。

## 权限和正文限制

- 扩展不绕过会员、付费、订阅或账号权限。
- 接口返回无权限或正文不完整时，扩展会停止读取并提示用户检查账号权限或登录状态。
- 不提供整本书下载、正文批量导出、跨用户共享或公开正文接口。

## 删除数据

在 VS Code 命令面板执行 `阅读：清除登录信息和缓存`，可以清除 SecretStorage 登录信息、本地阅读位置、书架与目录缓存、进程内缓存以及番茄小说本地章节和字体映射缓存。

卸载扩展后，用户仍可检查 VS Code 的扩展存储并按 VS Code 提供的方式清理残留本地数据。若需要彻底删除凭据，建议在卸载前先执行上述清除命令，并在对应阅读服务的账号安全设置中管理或退出相关登录会话。

## 风险提示

- 本扩展使用的部分能力可能依赖微信读书或番茄小说网页端内部、非公开接口和动态页面结构。
- 接口可能随时变化、限制或失效。
- 使用第三方工具、接口或登录信息的行为可能受到相关服务协议、账号规则和适用法律限制。
- 本隐私说明和免责声明不代表项目已获得腾讯、字节跳动或相关服务方授权，也不消除服务协议、商标或版权风险。
